# Pipeline Gap Status Matrix — 2026-07-05

Companion audit file for `docs/pipeline-state-reconciliation-20260705.md`.

This matrix maps every known quality gap to the desired behavior, current state, merged PRs, validation level, status, and next action.

Status values:

- `Closed by contract`: implementation exists and targeted contract/CI passed.
- `Partial`: guardrail/foundation exists, but desired production behavior is not fully implemented.
- `Open`: known product failure still appears or core implementation is missing.
- `Pending real-run validation`: implementation exists, but real draft output has not proven the invariant.

| Gap ID | Desired behavior | Current state | PRs merged | Validation level | Status | Next action |
|---|---|---|---|---|---|---|
| PQ-001 | Deterministic perception foundation exists for bbox/confidence/track metadata. | CV schema and Supervision adapter exist; #118 adds runtime sidecar ingestion; #120 adds a pre-analysis producer hook for configured external sidecar generation; #121 hardens stale skipped/failed sidecar handling and placeholder paths. A real production vision pass is still missing. | #88, #118, #120, #121 | Contract/CI only | Partial | Build/enable the production vision pass that writes sidecars, then validate on a real run. |
| PQ-002 | Identity clustering uses stable perception evidence and fails safe. | Identity fail-safe and real identity gate exist; #109 adds deterministic athlete IDs/merge evidence; #110 adds same-window multi-person review-required diagnostics; #116 wires that gate into the long-video context-QA upload path; #118 lets sidecar track IDs feed identity/visible-subject evidence when present; #120/#121 add and harden the pre-analysis producer hook. Real-run validation is still missing. | #90, #99, #106, #109, #110, #116, #118, #120, #121 | Contract/CI + prior real run still failing | Partial | Validate sidecar-backed identity behavior on a real run and build/configure the production sidecar evidence source. |
| PQ-003 | Crop/framing uses bbox/track center rather than Gemini hints. | BBox-driven crop guard exists when bbox data is available; #118 attaches sidecar bbox/crop metadata before runtime crop hardening; #120/#121 add and harden the producer hook. Gemini-only events still keep fallback behavior when no sidecar exists. | #89, #118, #120, #121 | Contract/CI only | Partial | Feed production sidecars into normal runs and fail/flag missing detections where required. |
| PQ-004 | Pipeline run processes only intended batch/session. | R2 batch isolation exists. | #91 | Contract/CI | Closed by contract | Verify in normal upload/start flow during next multi-file batch test. |
| PQ-005 | Same physical moment is not duplicated across sources. | Cross-source guard exists, and #109 adds athlete-level duplicate evidence only when strong IDs match; person-level duplicates still need real-run proof. | #93, #100, #102, #109 | Contract/CI + prior real run still shows duplicates | Partial | Validate athlete-level canonicalization against actual draft outputs. |
| PQ-006 | Weak filler events do not become normal drafts. | Parser/runtime weak moment guard exists. | #94 | Contract/CI | Pending real-run validation | Use candidate ledger to confirm good moments are not falsely dropped. |
| PQ-007 | Event windows include setup, peak/action, outcome or are flagged. | Window guard and cut-window guard exist; #111 adds per-ride `wave_completion_score`, `wave_boundary_evidence`, and review-required defects for missing/cut outcome evidence. | #95, #101, #106, #111 | Contract/CI only | Closed by contract | Validate score/evidence on the next real run outputs. |
| PQ-008 | Editor climax/teaser uses composite quality, not Gemini score alone. | Narrative quality guard exists, and #108 adds value labels/feedback schema but no ranker or replay eval yet. | #96, #108 | Contract/CI only | Partial | Build value eval set and candidate ranker using ledger/feedback events. |
| PQ-009 | Critical QA defects do not look like normal approvals. | QA diagnostics/fail-closed exist; #110/#111 add review-required metadata; #112 adds API/UI approval blocking; #113 hardens approval so direct API calls use storage-derived filenames before policy evaluation. | #97, #103, #104, #105, #110, #111, #112, #113 | Contract/CI only | Closed by contract | Validate in deployed operator UI after next Vercel build. |
| PQ-010 | Draft output is reconstructable from metadata. | Draft diagnostic artifact exists; #108 adds candidate ledger metadata; #110 persists `multi_person_clip_gate`; #111 persists `wave_completion_score` and `wave_boundary_evidence`; #112 exposes approval policy fields in draft list. | #98, #108, #110, #111, #112 | Contract/CI | Partial | Verify diagnostics in real artifacts and expose richer metadata in operator UI. |
| REAL-ID-001 | Mixed athletes do not reach normal review drafts. | Real identity gate exists; #109 adds run-level athlete IDs; #110 flags same-window multi-person drafts as review-required; #116 applies the same multi-person flagging to long-video context-QA drafts before upload; #118 lets sidecar `track_id` / visible-track evidence reach those guards when sidecars are present; #120/#121 add and harden the pre-analysis producer hook. Real-run proof is still missing. | #99, #106, #109, #110, #116, #118, #120, #121 | Contract/CI + prior real run still failing | Partial | Validate against real outputs and add/generate the production sidecar evidence source. |
| REAL-DUP-001 | Repeated segment does not appear twice in one draft. | Final duplicate guard exists. | #100 | Contract/CI | Pending real-run validation | Confirm with candidate/output ledger on next run. |
| REAL-CUT-001 | Surf waves are not cut before outcome. | Cut-window and surf ride guards exist; #111 now scores whether the draft window covers outcome evidence and marks `CUT_TOO_EARLY` / `WAVE_COMPLETION_UNCERTAIN` review-required. | #101, #106, #111 | Contract/CI only | Closed by contract | Validate against actual draft windows after the next run. |
| REAL-QA-001 | QA sees edit/source context, not only final reel. | Context QA sends edit/source JSON. | #102, #104 | Contract/CI | Closed by contract | Verify UI exposes QA reasons to operator. |
| REAL-DUP-002 | Duplicate rendered drafts under different descriptions are blocked. | Run-level context dedup exists; #109 adds stable athlete IDs and duplicate evidence for strong matches, but prior real run still failed at athlete level. | #102, #109 | Contract/CI + pending real-run validation | Partial | Validate strong-match athlete merging and collection behavior on real outputs. |
| REAL-QA-002 | QA uncertainty fails closed. | Skipped/unavailable QA becomes review-required FAIL; #112/#113 now block normal approval for review-required drafts in operator/API paths. | #103, #112, #113 | Contract/CI | Closed by contract | Verify deployed UI blocks normal approval for review-required drafts. |
| REAL-QA-004 | QA can compare final draft against source-window video evidence. | Source evidence clips are uploaded when available and fail-closed when upload fails. | #105 | Contract/CI | Pending real-run validation | Persist whether each draft had `source_evidence_visual_uploaded`. |
| REAL-WAVE-001 | Surf ride is atomic editorial unit. | Surf fragments normalize into `surf_ride`; #111 attaches completion scoring to the normalized ride. | #106, #111 | Contract/CI | Closed by contract | Validate merged ride scoring on next real run. |
| REAL-ID-003 | Similar surfers inside source windows are not silently merged. | Identity uncertainty can be surfaced; #109 avoids weak/description-only canonical merges; #110 flags same-window multi-person visibility; #116 extends long-video upload-path flagging; #118 attaches sidecar visible-track IDs to event windows when available; #120/#121 add and harden the pre-analysis producer hook. No real-run proof yet. | #106, #109, #110, #116, #118, #120, #121 | Contract/CI + prior real run still failing | Partial | Validate same-window behavior and add stable sidecar-backed athlete registry. |
| REAL-ID-004 | Single-athlete draft does not contain another visible athlete unless intentional. | #110 adds `multi_person_clip_gate`: extra visible subject IDs become `MULTI_PERSON_CLIP` review-required unless the event is explicitly `SOCIAL_MOMENT` / `HIGH_FIVE` / intentional multi-person. #116 wires the same gate into long-video context-QA before draft naming/upload, #118 can feed `source_window_track_ids` from sidecars when present, and #120/#121 add and harden the producer hook. Not real-run validated yet. | #110, #116, #118, #120, #121 | Contract/CI only | Closed by contract | Validate against the next real run and sidecar-backed visible-subject evidence. |
| REAL-WAVE-002 | Every surf draft exposes wave completion confidence. | #111 adds `wave_completion_score`, structured `wave_boundary_evidence` for ride_start / peak_action / ride_end, and review-required defects for missing or cut outcome evidence. Not real-run validated yet. | #111 | Contract/CI only | Closed by contract | Validate score/evidence against the next real run drafts. |
| REAL-VALUE-001 | System learns what is worth showing from feedback and examples. | #108 adds value labels and operator feedback event schema, but there is no feedback dataset, replay eval, or product-value ranker yet. | #108 | Contract/CI only | Partial | Build eval set, replay grader, and value ranker from reviewed runs. |
| REAL-RECALL-001 | Good moments are not silently dropped. | #108 adds candidate decision ledger/false-negative schema, but the high-five/social miss has not been re-run or explained yet. | #108 | Contract/CI + pending real-run validation | Pending real-run validation | Inspect next run ledger for detected/selected/dropped social moments. |
| REAL-ATHLETE-001 | Same athlete maps to stable `athlete_id` and does not create many standalone drafts. | #109 assigns stable `athlete_id`, merges only strong same-athlete evidence, marks merged same-athlete groups as collections, and emits duplicate evidence; not real-run validated yet. | #109 | Contract/CI only | Closed by contract | Validate on next real run and expand to sidecar-backed canonicalization. |
| REAL-UI-001 | QA/review-required drafts cannot be approved like normal drafts. | #112 makes `/api/operator/drafts` expose approval policy metadata, disables Approve in mobile Review, and shows blocking reasons; #113 routes approval through a storage-derived filename policy handler so direct API calls cannot bypass the QA block with a clean `file_name`. | #112, #113 | Contract/CI only | Closed by contract | Verify in deployed UI after Vercel quota/build succeeds. |
| REAL-UPLOAD-001 | Upload button supports selecting/uploading many videos in one action. | #115 adds multi-file upload initialization with shared `batch_id`, keeps single-file compatibility, adds operator UI multi-select/concurrent upload/per-file progress/retry, and extends upload/batch contracts. | #115 | Contract/CI only | Closed by contract | Validate with a real multi-file operator upload only after audit/gap files remain synchronized and user approves a real run. |
| REAL-TRACE-001 | Every candidate has decision trace. | #108 adds `candidate_decision_ledger` and feedback schema to draft diagnostics, including selected/review-required/QA-blocked/dropped entries where surfaced by the artifact. | #108 | Contract/CI only | Pending real-run validation | Verify ledger completeness in the next real run artifacts and UI metadata. |

## Immediate implication

The ledger foundation, athlete canonicalization, same-window multi-person guard, wave completion scoring, operator approval blocking, multi-file batch upload, sidecar-ingestion path, and pre-analysis producer hook are now implemented by contract. #121 hardens the producer hook against stale skipped/failed sidecars, but the active repair order is still:

1. Generate/enable real sidecars, then validate all perception-dependent quality claims with a real pipeline run only after audit/gap files remain synchronized and the user approves the run.
2. Continue deeper product-quality work on `REAL-VALUE-001` / replay eval and ranking without claiming real-run validation yet.
