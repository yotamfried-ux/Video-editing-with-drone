# Pipeline Gap Status Matrix — 2026-07-05

Companion audit file for `docs/pipeline-state-reconciliation-20260705.md`.

This matrix maps every known quality gap to the desired behavior, current state, merged PRs, validation level, status, and next action.

Status values:

- `Closed by contract`: implementation exists and targeted contract/CI passed.
- `Partial`: guardrail/foundation exists, but desired production behavior is not fully implemented.
- `Open`: known product failure still appears or core implementation is missing.
- `Pending real-run validation`: implementation exists, but real draft output has not proven the invariant.

| Gap ID | Desired behavior | Current state | PRs merged | Validation level | Status | Next action |
|---|---|---|---|---|---|---|
| PQ-001 | Deterministic perception foundation exists for bbox/confidence/track metadata. | CV schema and Supervision adapter exist; #118 adds runtime sidecar ingestion; #120 adds a pre-analysis producer hook for configured external sidecar generation; #121 hardens stale skipped/failed sidecar handling and placeholder paths; #123 adds a first-party external-JSON sidecar producer adapter/CLI. A real production detector/tracker backend is still not configured or real-run validated. | #88, #118, #120, #121, #123 | Contract/CI only | Partial | Configure a real detector/tracker backend to feed the sidecar producer, then validate on a real run. |
| PQ-002 | Identity clustering uses stable perception evidence and fails safe. | Identity fail-safe and real identity gate exist; #109 adds deterministic athlete IDs/merge evidence; #110 adds same-window multi-person review-required diagnostics; #116 wires that gate into the long-video context-QA upload path; #118 lets sidecar track IDs feed identity/visible-subject evidence when present; #120/#121 add and harden the pre-analysis producer hook; #123 adds a command adapter that can normalize external track evidence. Real-run validation is still missing. | #90, #99, #106, #109, #110, #116, #118, #120, #121, #123 | Contract/CI + prior real run still failing | Partial | Validate sidecar-backed identity behavior on a real run after configuring actual detector/tracker output. |
| PQ-003 | Crop/framing uses bbox/track center rather than Gemini hints. | BBox-driven crop guard exists when bbox data is available; #118 attaches sidecar bbox/crop metadata before runtime crop hardening; #120/#121 add and harden the producer hook; #123 adds the sidecar producer adapter/CLI. Gemini-only events still keep fallback behavior when no real sidecar exists. | #89, #118, #120, #121, #123 | Contract/CI only | Partial | Feed production sidecars into normal runs and fail/flag missing detections where required. |
| PQ-004 | Pipeline run processes only intended batch/session. | R2 batch isolation exists. | #91 | Contract/CI | Closed by contract | Verify in normal upload/start flow during next multi-file batch test. |
| PQ-005 | Same physical moment is not duplicated across sources. | Cross-source guard exists, and #109 adds athlete-level duplicate evidence only when strong IDs match; person-level duplicates still need real-run proof. | #93, #100, #102, #109 | Contract/CI + prior real run still shows duplicates | Partial | Validate athlete-level canonicalization against actual draft outputs. |
| PQ-006 | Weak filler events do not become normal drafts. | Parser/runtime weak moment guard exists. | #94 | Contract/CI | Pending real-run validation | Use candidate ledger to confirm good moments are not falsely dropped. |
| PQ-007 | Event windows include setup, peak/action, outcome or are flagged. | Window and cut-window guards exist; #111 adds per-ride completion evidence. Open PR #182 removes the historical 15-second cap for complete surf rides, preserves complete-action boundaries into QA evidence, and blocks an individually over-budget action instead of truncating it. #182 is not merged and has no real-footage proof. | #95, #101, #106, #111 | Open PR #182 contract/CI; real run pending | Pending real-run validation | After explicit merge approval, run the same footage as `29516256449` and compare source windows, rendered timelines, and final videos. |
| PQ-008 | Editor climax/teaser uses composite quality, not Gemini score alone. | Narrative quality guard exists, and #108 adds value labels/feedback schema but no ranker or replay eval yet. | #96, #108 | Contract/CI only | Partial | Build value eval set and candidate ranker using ledger/feedback events. |
| PQ-009 | Critical QA defects do not look like normal approvals. | QA diagnostics/fail-closed exist; #110/#111 add review-required metadata; #112/#113 block approval. #182 (merged as `e941f89e`) requires explicit final QA PASS evidence, isolates QA state per invocation, persists server-side publishability authority, and binds listing/approval to the immutable Drive file ID or canonical R2 object key rather than a reusable filename. `20260717_draft_publishability_authority.sql` is now applied and live-verified (2026-07-19: DRY_RUN `29698171717`, APPLY via authenticated Supabase MCP, `list_tables`/`pg_indexes` confirm table/PK/indexes/RLS). | #97, #103, #104, #105, #110, #111, #112, #113, #182 | Merged + migration applied/live-verified; real run pending | Pending real-run validation | Run production footage and verify missing/FAIL QA is blocked consistently in API and mobile Review. |
| PQ-010 | Draft output is reconstructable from metadata. | Draft diagnostic artifact exists; #108 adds candidate ledger metadata; #110 persists `multi_person_clip_gate`; #111 persists wave completion evidence; #112 exposes approval policy fields. Open PR #182 adds a strict per-athlete publishable manifest with action lineage, final QA evidence, media snapshot, upload identity, and terminal business-gate evidence. | #98, #108, #110, #111, #112 | Open PR #182 contract/CI; artifact inspection pending | Pending real-run validation | Inspect #182 production artifacts end to end after explicit merge/run approval and confirm each Review object traces to source/action/QA/authority state. |
| REAL-ID-001 | Mixed athletes do not reach normal review drafts. | Real identity gate exists; #109 adds run-level athlete IDs; #110 flags same-window multi-person drafts as review-required; #116 applies the same multi-person flagging to long-video context-QA drafts before upload; #118 lets sidecar `track_id` / visible-track evidence reach those guards when sidecars are present; #120/#121 add and harden the pre-analysis producer hook; #123 adds a producer adapter for external track evidence. Real-run proof is still missing. | #99, #106, #109, #110, #116, #118, #120, #121, #123 | Contract/CI + prior real run still failing | Partial | Validate against real outputs after configuring actual detector/tracker sidecar generation. |
| REAL-DUP-001 | Repeated segment does not appear twice in one draft. | Final duplicate guard exists. | #100 | Contract/CI | Pending real-run validation | Confirm with candidate/output ledger on next run. |
| REAL-CUT-001 | Surf waves are not cut before outcome. | Cut-window and surf ride guards exist; #111 scores outcome coverage. Open PR #182 preserves every complete usable ride, removes the single-clip 15-second cap, carries the real rendered window into QA, and splits only between whole actions below the 90-second ceiling. | #101, #106, #111 | Open PR #182 contract/CI; real run pending | Pending real-run validation | Visually compare every usable ride from run `29516256449` against the new Parts and prove no start/end truncation. |
| REAL-QA-001 | QA sees edit/source context, not only final reel. | Context QA sends edit/source JSON. | #102, #104 | Contract/CI | Closed by contract | Verify UI exposes QA reasons to operator. |
| REAL-DUP-002 | Duplicate rendered drafts under different descriptions are blocked. | Run-level context dedup exists; #109 adds stable athlete IDs and duplicate evidence for strong matches, but prior real run still failed at athlete level. | #102, #109 | Contract/CI + pending real-run validation | Partial | Validate strong-match athlete merging and collection behavior on real outputs. |
| REAL-QA-002 | QA uncertainty fails closed. | Skipped/unavailable QA becomes review-required FAIL; #112/#113 block normal approval. Open PR #182 removes inferred PASS from an empty flagged set, requires `qa_evidence_recorded:true` plus `qa_verdict:PASS`, consumes evidence atomically by invocation token, and prevents a later runtime patch from replacing that scope. | #103, #112, #113 | Open PR #182 contract/CI; real run pending | Pending real-run validation | Exercise PASS, FAIL, unavailable QA, repaired-output re-grade, and two matching athlete labels in a real pipeline/operator run. |
| REAL-QA-004 | QA can compare final draft against source-window video evidence. | Source evidence clips are uploaded when available and fail-closed when upload fails. | #105 | Contract/CI | Pending real-run validation | Persist whether each draft had `source_evidence_visual_uploaded`. |
| REAL-WAVE-001 | Surf ride is atomic editorial unit. | Surf fragments normalize into `surf_ride`; #111 attaches completion scoring. Open PR #182 makes the complete ride the packing/render/QA unit, allows another surfer on the same wave while the target remains central, and forbids splitting/truncating a ride for Part limits. | #106, #111 | Open PR #182 contract/CI; real run pending | Pending real-run validation | Run the same source footage as `29516256449` and prove every usable wave appears exactly once or has a hard-reject record. |
| REAL-ID-003 | Similar surfers inside source windows are not silently merged. | Identity uncertainty can be surfaced; #109 avoids weak/description-only canonical merges; #110 flags same-window multi-person visibility; #116 extends long-video upload-path flagging; #118 attaches sidecar visible-track IDs to event windows when available; #120/#121 add and harden the producer hook; #123 adds the external-JSON sidecar producer adapter. No real-run proof yet. | #106, #109, #110, #116, #118, #120, #121, #123 | Contract/CI + prior real run still failing | Partial | Validate same-window behavior after configuring a real detector/tracker backend and expand to sidecar-backed athlete registry. |
| REAL-ID-004 | Single-athlete draft does not contain another visible athlete unless intentional. | #110 adds `multi_person_clip_gate`; #116 wires it into long-video context-QA; #118 can feed sidecar visible-track IDs; #120/#121 add and harden the producer hook; #123 adds the producer adapter. Open PR #182 supersedes the literal single-visible-person interpretation with a centered featured-athlete contract: surrounding people are allowed only with positive identity, centrality, continuity, confidence, and action-ownership evidence. | #110, #116, #118, #120, #121, #123 | Open PR #182 contract/CI; detector-backed real run pending | Pending real-run validation | Validate football group play and shared-wave surfing with real detector/tracker evidence; confirm target loss and identity switch remain blocked. |
| REAL-WAVE-002 | Every surf draft exposes wave completion confidence. | #111 adds `wave_completion_score` and boundary evidence. Open PR #182 preserves the complete-action window through rendering and final QA and blocks publication without explicit final evidence. | #111 | Open PR #182 contract/CI; real run pending | Pending real-run validation | Validate wave boundaries, QA timestamps, and final media duration against real source footage. |
| REAL-VALUE-001 | System learns what is worth showing from feedback and examples. | #108 adds value labels and operator feedback event schema, but there is no feedback dataset, replay eval, or product-value ranker yet. | #108 | Contract/CI only | Partial | Build value eval set, replay grader, and value ranker from reviewed runs. |
| REAL-RECALL-001 | Good moments are not silently dropped. | #108 adds candidate decision ledger/false-negative schema. Open PR #182 adds a strict per-athlete/all-usable-wave coverage gate, but the high-five/social miss and footage-level recall have not been re-run. | #108 | Open PR #182 contract/CI; real run pending | Pending real-run validation | Inspect the next candidate ledger and final manifest for every detected/selected/dropped action and compare directly with source footage. |
| REAL-ATHLETE-001 | Same athlete maps to stable `athlete_id` and does not create many standalone drafts. | #109 assigns stable IDs and strong-evidence collections. Open PR #182 requires one primary publishable reel per eligible athlete, permits Parts only when complete actions exceed the 90-second budget, rejects duplicate athlete ownership, and reconciles the final manifest to canonical coverage. | #109 | Open PR #182 contract/CI; real run pending | Pending real-run validation | After merge approval, prove every eligible athlete receives one primary reel, no athlete owns duplicate primary outputs, and Parts reflect only real duration pressure. |
| REAL-UI-001 | QA/review-required drafts cannot be approved like normal drafts. | #112/#113 expose and enforce approval policy. #182 (merged as `e941f89e`) adds authoritative server-side publishability rows and storage-object-only lookup; R2 now returns the same canonical key listed by the operator API, removing filename fallback. `draft_publishability` is applied and live-verified. Production Vercel deployment is currently pinned to the #182 merge commit; PR #183 and later `main` commits have not promoted (each later production deploy attempt shows `CANCELED`). | #112, #113, #182 | Merged + migration applied/live-verified; deployed smoke pending | Pending real-run validation | Confirm/redeploy `main` to production if PR #183 or later commits are required, then verify direct API and mobile approval both fail closed for missing/mismatched/failed authority. |
| REAL-UPLOAD-001 | Upload button supports selecting/uploading many videos in one action. | #115 adds multi-file upload initialization with shared `batch_id`, keeps single-file compatibility, adds operator UI multi-select/concurrent upload/per-file progress/retry, and extends upload/batch contracts. | #115 | Contract/CI only | Closed by contract | Validate with a real multi-file operator upload only after audit/gap files remain synchronized and user approves a real run. |
| REAL-UPLOAD-002 | Operator can upload videos directly from a connected SD card or USB storage without importing them into the media gallery first. | PR #183 adds an Android Storage Access Framework folder picker, filters supported video files, copies one source at a time into app cache immediately before the existing verified R2 upload, preserves shared batch/progress/retry behavior, and removes the temporary cache copy in a `finally` block. | #183 pending merge | Implementation + targeted contract; physical-device validation pending | Pending real-run validation | After merge, deploy the mobile JavaScript update and validate folder selection plus verified upload with a real Android phone, card reader, and SD card before starting the pipeline. |
| REAL-TRACE-001 | Every candidate has decision trace. | #108 adds `candidate_decision_ledger`. Open PR #182 adds per-athlete action lineage, final QA invocation evidence, immutable upload identity, media specs, business-gate result, and explicit hard-reject/blocking reasons. | #108 | Open PR #182 contract/CI; artifact inspection pending | Pending real-run validation | Verify ledger/manifest completeness and operator traceability in the first post-merge production run. |

## Immediate implication

The ledger foundation, athlete canonicalization, same-window multi-person guard, wave completion scoring, operator approval blocking, multi-file batch upload, sidecar-ingestion path, pre-analysis producer hook, and first-party sidecar producer adapter are implemented on `main` by contract. PR #182 (merged as `e941f89e0b9aa94c21d5d68f9e9da17a38012021`) and PR #183 (direct Android SD/USB folder upload, merged as `3048538edaa8a89f68eb94682b8b63a8584003ca`) are both merged into `main`. `20260717_draft_publishability_authority.sql` has been applied to the live `bcndgmymnismbxvdeetc` database and live-verified (2026-07-19). Production Vercel is currently deployed at the #182 merge commit exactly; every later attempt to promote `main` (including #183's commit and the "EAS publication" commits after it) shows a `CANCELED` deployment, so PR #183 and later `main` commits are **not yet live in production**. Neither #182 nor #183 is real-footage validated. Therefore:

1. Do not mark the centered-athlete, per-athlete coverage, complete-wave, silent-output, explicit-QA, or storage-authority outcomes closed from CI/migration evidence alone.
2. Confirm/trigger a production deployment for the current `main` tip if PR #183 or later commits are required in production; then run the same footage as `29516256449` and inspect every required artifact/video.
3. Configure a real detector/tracker backend for perception-dependent identity/crop claims; #182 does not remove that dependency.
4. Continue deeper product-value work on `REAL-VALUE-001` / replay eval and ranking without claiming real-run validation yet.
