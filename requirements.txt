google-genai>=1.0.0
langsmith>=0.1.147
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.2.0
python-dotenv>=1.0.0
sentry-sdk>=2.0.0
supabase>=2.0.0
boto3>=1.34.0
librosa>=0.11.0
soundfile>=0.12
Pillow>=10.0.0
torch>=2.0.0
transformers>=4.35.0
supervision>=0.29.0,<0.30.0

# Face recognition for athlete matching (128-float embeddings).
# dlib compiles from source — needs cmake + a C++ toolchain (build-essential).
# CI installs cmake via apt before `pip install -r requirements.txt`.
face_recognition>=1.3.0

# ffmpeg must be installed separately via system package manager:
#   Ubuntu/Debian: sudo apt install ffmpeg
#   macOS:         brew install ffmpeg
