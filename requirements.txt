google-genai>=1.0.0
langsmith>=0.1.147
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.2.0
python-dotenv>=1.0.0
sentry-sdk>=2.0.0
supabase>=2.0.0
boto3>=1.34.0
librosa>=0.11.0
soundfile>=0.12
Pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0
lap>=0.5.12
transformers>=4.35.0
supervision>=0.29.0,<0.30.0
ultralytics>=8.3.0

# Face recognition for athlete matching.
# dlib compiles from source and needs cmake plus a C++ toolchain.
# CI installs cmake before pip install -r requirements.txt.
face_recognition>=1.3.0

# ffmpeg must be installed separately by the system package manager.
