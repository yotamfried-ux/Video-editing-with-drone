name: Publish Specific SD Video Selection Update

# One-time operational publication after PR #187 was merged and safely validated.
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/publish-specific-sd-video-selection-update.yml'

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mobile/package-lock.json

      - name: Install mobile dependencies
        working-directory: mobile
        run: npm ci --legacy-peer-deps

      - name: Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Publish Android OTA updates
        id: publish
        working-directory: mobile
        env:
          EXPO_PUBLIC_SUPABASE_URL: 'https://bcndgmymnismbxvdeetc.supabase.co'
          EXPO_PUBLIC_SUPABASE_ANON_KEY: 'sb_publishable_mwAM5lvgulv3sCtrHm-OkA_4wMCpNZJ'
          EXPO_PUBLIC_API_BASE_URL: 'https://video-editing-with-drone.vercel.app'
          EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY: 'pk_test_51TgXCICwQyJBd9MdnZum7LfttimmjbMwlys25dS7GGXDj4B9aX5uWla3sN84KkpaKpX3txVPIr0y65UygDVH6OUG007rD31Tyq'
        shell: bash
        run: |
          set +e
          eas update \
            --channel preview \
            --platform android \
            --message 'Specific SD and USB video selection (#187)' \
            --json \
            --non-interactive \
            > /tmp/eas-preview.json 2> /tmp/eas-preview.err
          preview_status=$?

          eas update \
            --channel production \
            --platform android \
            --message 'Specific SD and USB video selection (#187)' \
            --json \
            --non-interactive \
            > /tmp/eas-production.json 2> /tmp/eas-production.err
          production_status=$?
          set -e

          echo "preview_status=$preview_status" >> "$GITHUB_OUTPUT"
          echo "production_status=$production_status" >> "$GITHUB_OUTPUT"

      - name: Record publication evidence and clean up
        shell: bash
        env:
          PREVIEW_STATUS: ${{ steps.publish.outputs.preview_status }}
          PRODUCTION_STATUS: ${{ steps.publish.outputs.production_status }}
        run: |
          mkdir -p docs/audit
          {
            echo '# EAS publication — specific SD / USB video selection'
            echo
            echo 'Date: 2026-07-20'
            echo 'Source merge: PR #187 / commit `427e95faed7e2fdf523e8c85214083b4faa4e304`'
            echo 'Platform: Android'
            echo
            echo "- Preview channel exit status: \`$PREVIEW_STATUS\`"
            echo "- Production channel exit status: \`$PRODUCTION_STATUS\`"
            echo
            echo '## Preview result'
            echo '```json'
            cat /tmp/eas-preview.json 2>/dev/null || true
            echo '```'
            if [ "$PREVIEW_STATUS" != '0' ]; then
              echo
              echo '### Preview error'
              echo '```text'
              tail -n 80 /tmp/eas-preview.err 2>/dev/null || true
              echo '```'
            fi
            echo
            echo '## Production result'
            echo '```json'
            cat /tmp/eas-production.json 2>/dev/null || true
            echo '```'
            if [ "$PRODUCTION_STATUS" != '0' ]; then
              echo
              echo '### Production error'
              echo '```text'
              tail -n 80 /tmp/eas-production.err 2>/dev/null || true
              echo '```'
            fi
            echo
            if [ "$PREVIEW_STATUS" = '0' ] && [ "$PRODUCTION_STATUS" = '0' ]; then
              echo 'Status: **published successfully to both channels**.'
            else
              echo 'Status: **publication failed on at least one channel**.'
            fi
          } > docs/audit/eas-update-specific-sd-video-selection-20260720.md

          if [ "$PREVIEW_STATUS" = '0' ] && [ "$PRODUCTION_STATUS" = '0' ]; then
            rm .github/workflows/publish-specific-sd-video-selection-update.yml
          fi

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/audit/eas-update-specific-sd-video-selection-20260720.md
          git add -u .github/workflows/publish-specific-sd-video-selection-update.yml || true
          git commit -m 'Record EAS publication for specific SD video selection'
          git pull --rebase origin main
          git push origin HEAD:main

          if [ "$PREVIEW_STATUS" != '0' ] || [ "$PRODUCTION_STATUS" != '0' ]; then
            exit 1
          fi
