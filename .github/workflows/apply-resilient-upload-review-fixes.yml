name: Apply Resilient Upload Review Fixes

on:
  pull_request:
    branches: [main]
    paths:
      - 'scripts/apply_resilient_upload_review_fixes.py'
      - '.github/workflows/apply-resilient-upload-review-fixes.yml'

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: |
            mobile/package-lock.json
            web-api/package-lock.json

      - name: Apply P1 review fixes
        run: python scripts/apply_resilient_upload_review_fixes.py

      - name: Commit review fixes
        shell: bash
        run: |
          rm scripts/apply_resilient_upload_review_fixes.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            'mobile/src/app/(operator)/pipeline.tsx' \
            'scripts/test_resilient_upload_contract.py' \
            'docs/audit/resilient-r2-upload-retry-plan.md'
          git add -u scripts/apply_resilient_upload_review_fixes.py
          git commit -m 'Address resilient upload review findings'
          git push origin "HEAD:${{ github.head_ref }}"

      - name: Validate resilient upload contract
        run: python scripts/test_resilient_upload_contract.py

      - name: Validate external storage upload contract
        run: python scripts/test_external_storage_upload_contract.py

      - name: Validate storage contract
        run: python scripts/test_storage_contract.py

      - name: Validate operator workflow
        run: python scripts/validate_operator_smoke_workflow.py

      - name: Validate operator workflow validator
        run: python scripts/test_operator_smoke_workflow_validator.py

      - name: Validate mobile TypeScript
        working-directory: mobile
        run: |
          npm ci --legacy-peer-deps
          npm run type-check

      - name: Validate Web API TypeScript
        working-directory: web-api
        run: |
          npm ci
          npm run type-check
